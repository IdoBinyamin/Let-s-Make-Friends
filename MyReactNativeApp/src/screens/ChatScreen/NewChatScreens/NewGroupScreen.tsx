import {
	StyleSheet,
	Text,
	View,
} from 'react-native';
import React from 'react';

type Props = {};

export const NewGroupScreen = (props: Props) => {
	return (
		<View>
			<Text>NewGroupScreen</Text>
		</View>
	);
};

const styles = StyleSheet.create({});
